# 教材资源详情页 · 改造成果说明

> 基于《教材资源详情页优化方案》，第一阶段已落地实现。本文档说明改动内容、如何使用、以及后续待办。

---

## 一、改了什么

### 1. 详情页从"简陋卡片"升级为"备课级学习页"
原来点击「查看」只是一个小弹窗，显示标题、节选原文、标签、重点、考点。
现在改为**右侧抽屉 + 7 个标签页**：

| 标签页 | 内容 | 数据来源 |
|---|---|---|
| 📄 全文 | 完整正文、分段、背诵高亮、朗读、复制、字号调节 | `fullText`（缺则降级 `original`） |
| 🔤 拼音 | 原文生僻字注音（可开/关）、本篇注音词表 | `pinyinMap` + 通用拼音表 |
| 📝 注释 | 重点字词释义 + 文言知识点（通假/古今异义/词类活用/特殊句式） | `notes` / `wenyan` |
| 🌐 译文 | 全文翻译或重点句翻译 | `translation` / `hardSentences.translation` |
| 💡 讲解 | 重难点句子逐句卡片：原句 + 翻译 + 赏析 + 考点标签 | `hardSentences` |
| 🎨 赏析 | 主题思想、结构思路、艺术特色、名句赏析 | `appreciation` |
| 🎯 考点 | 默写名句、常见题型 | `examFocus` |

### 2. 新增文件
- `js/modules/pinyin-data.js` — 通用生僻字拼音表（**225 字**）+ 异读字提示表。

### 3. 扩展文件
- `js/utils.js` — 新增 `htmlEncode`、`isHanChar`、`splitParagraphs`、`annotatePinyin`（拼音标注）、`highlightHardSentences`（重难点高亮）、`extractFamousLines`。
- `js/modules/resource.js` — 重写详情页为标签页组件，新增朗读/复制/字号调节/背诵高亮等功能。
- `css/style.css` — 新增抽屉、标签页、ruby 拼音、句子卡片、明暗主题全套样式。
- `index.html` — **修复原项目遗漏**：补加 `<script src="js/utils.js">`（此前该文件从未被加载，本改造依赖它）。
- `data/sample-data.js` — 为《劝学》《登高》《沁园春·长沙》三篇补齐 `fullText`、`pinyinMap`、`notes`、`hardSentences`、`wenyan`、`appreciation`、`examFocus` 作为示范。

---

## 二、怎么看效果

1. 双击 `index.html` 打开软件（无需启动服务器，纯静态即可）。
2. 左侧进入「📁 教材资源」。
3. 在「全部 / 课文 / 文言文 / 古诗词」任一标签下，点卡片上的「📖 查看」（文言文为「📖 查看」、古诗词为「📝 赏析」按钮，均已指向新详情页）。
4. 右侧抽屉弹出，顶部可切换 7 个标签页，右上角有🔊朗读、📋复制、➕备课、✕关闭。

**推荐先看这三篇示范**（数据最完整）：
- 《劝学》— 文言文，7 个标签全部有内容（含文言知识点、4 句讲解）。
- 《登高》— 古诗词，含名句讲解与赏析。
- 《沁园春·长沙》— 现代课文，补全了完整上下阕。

---

## 三、降级兼容（重要）

对所有**尚未补充新字段**的篇目（如《百合花》《故都的秋》等），详情页会：
- 全文页：显示 `original`（多为节选），并提示「⚠️ 当前展示为节选，全文待补充」。
- 拼音页：仅对命中通用拼音表的字注音，无篇目词表时提示「本篇暂未配置生僻字注音表」。
- 注释/译文/讲解/赏析/考点：显示「📭 本篇暂未配置 XX 数据」，不会报错或白屏。

即：新能力「有数据才显示，没数据不报错」，可安全逐步补充。

---

## 四、已验证

- `node --check` 对 `utils.js`、`pinyin-data.js`、`resource.js`、`sample-data.js` 全部通过。
- 纯函数逻辑测试通过：拼音标注（篇目级优先 + 通用兜底）、分段、HTML 转义、重难点高亮均正确。
- 示范数据结构完整；未补充数据篇目降级逻辑正确。

---

## 五、后续待办（对应方案第二阶段起）

1. **补齐 132 篇数据**：优先高考高频篇目，逐步补 `fullText`（全文）、`pinyinMap`、`hardSentences` 等。可借助文本来源 + 人工校准，或接入大模型辅助生成（需人工审核）。
2. **拼音准确性**：通用表覆盖单音生僻字；多音/异读/通假字务必在篇目级 `pinyinMap` 精确标注（已预留）。
3. **打通关联模块**：与「备课中心」联动（一键生成含原文+拼音+讲解的讲义）；与「试卷中心」联动（自动出默写/翻译题）。
4. **移动端/响应式**：当前详情页按桌面端设计，窄屏需进一步适配。

---

## 六、数据补充速查（单篇对象新增字段示例）

```js
'篇名': {
  // …原有字段…
  fullText: '完整课文正文，段落用 \\n 分隔',
  pinyinMap: { '輮': 'róu', '中': 'zhòng' },       // 篇目级精确注音（含异读/通假）
  notes: { '輮': '使…弯曲（通“煣”）' },            // 字词释义
  hardSentences: [                                 // 重难点句子讲解
    { sentence: '原句', translation: '翻译', explanation: '赏析', tags: ['比喻','默写'] }
  ],
  wenyan: { tongjia:[], gjyi:[], cihuolei:[], specialSentence:[], keyWords:[] },
  appreciation: { theme:'', structure:'', features:[], famousLines:[] },
  examFocus: { dictation:[], questionTypes:[] }
}
```
