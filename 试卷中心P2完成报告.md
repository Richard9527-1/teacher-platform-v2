# 试卷中心 P2 完成报告（套卷结构）

> 日期：2026-08-04
> 范围：`js/modules/exam.js` —— 方案《试卷中心改造方案.md》P2「套卷结构配置 + 难度/年级筛选」
> 关联：P0（能出/出对）、P1（题目好）已完成；本次在前两者基础上让组卷"好用"。

## 一、目标

让「试卷中心」从"一次只能出一种题型"升级为**可自由组合的混合套卷**，并支持按年级/单元/难度筛选课文，从而真正落地"组卷"能力（方案 P2 第 9 项）。

## 二、落地内容

| 能力 | 实现 | 说明 |
| --- | --- | --- |
| 混合套卷结构 | `generateMixedPaper(textNames, sections, clozeMode)` | `sections = [{type, count, score}]`，多题型 × 数量 × 分值自由组合，按大题聚合、累计总分 |
| 单题型生成器复用 | 抽出 `generateSectionQuestions` | 旧 `generateQuestionsFromTexts` 保留为兼容入口，P1 回归脚本无影响 |
| 试卷结构 UI | 重写 `renderExam` | 默认 3 大题（选择题×10×3 / 默写×6×5 / 赏析×2×10）；支持增/删题型行、改题量/分值；实时合计题数/分值 |
| 默写模式联动 | `syncClozeVisibility` | 仅当结构含"默写"大题时显示默写模式（直接/理解性）切换 |
| 年级/单元/难度筛选 | `getTextMeta` + `applyTextFilter` | 年级/单元取自索引；难度由年级派生（高一=基础/高二=提高/高三=培优），联动收窄课文列表 |
| 分节渲染 | `renderQuestions` 兼容 `paper` | 卷头总分信息 + "第X大题 题型（每小题Y分，共Z分）"标题 + 跨大题连续编号；0 产出大题给提示 |
| 分节导出 | `exportToWord`/`exportToJson` 兼容 `paper` | Word 按大题标题分页含满分信息；JSON 输出 `meta + sections[]` |
| 失败兜底 | `renderQuestions` 展示 `paper.error` | 全部大题 0 产出时直接显示"⚠️ 组卷失败：…"，不丢错误原因 |

## 三、关键修复 / 设计决定

- **向后兼容**：`generateQuestionsFromTexts(textNames, type, count, clozeMode)` 语义不变（返回扁平题数组），`_p1_test.js` 直接复用，未回归。
- **类型匹配在混合场景下依然正确**：翻译题仅在非课文篇目生成、现代文阅读仅在课文篇目生成；套卷中即使混选文体，各题型也只从匹配篇目产出，不匹配者该大题 0 产出并提示，不崩溃。
- **题量尽力产出**：若所选篇目难句/赏析数据不足，某大题实际题数可能低于请求数（如现代文阅读 1/3），UI 与导出均标注"应 M 题 / 实 N 题"，不强行凑数。

## 四、校验结果

- `node --check js/modules/exam.js` 通过。
- `_p2_test.js`（VM 实跑）：`getTextMeta` 元数据、难度筛选与年级一致性、7 大题混合套卷（总分/分值一致、大题标题格式）、翻译/现代文阅读类型匹配、理解性默写 mode、类型不匹配边界、空配置防御——**全部通过**。
- 重跑 `_p1_test.js`：149 题、**0 异常 / 0 占位 / 0 翻译无采分点 / 0 多选非数组**，确认无回归。

## 五、内容抽检（节选）

- 混合套卷示例：选择题 6、多选题 3、默写 4（理解性）、翻译 2、赏析 2、主观题 2、现代文阅读 1 → 全卷 20 题 / 112 分（应 23 题）。
- 理解性默写题面含真实情境（取自难句解析/主题），如"在《劝学》中，围…"引导填原句。

## 六、留待后续（方案 P2 其余条目，本次未做）

1. ~~答案卷分离 + 规范 `.docx`~~ —— **已完成**（见第七节）。
2. 试卷保存 / 命名 / 历史（`localStorage` 持久化、可复用二次编辑）。
3. 生成后单题微调（删题 / 调序 / 替换 / 收藏）。
4. 更多题型扩展（文言词语解释/断句、开放探究、微写作/作文）。

## 七、补充：答案卷分离 + 规范 Word 导出（2026-08-04 已完成）

- 重构 `exportToWord`：抽出纯函数 `buildWordHtml(paper, opts)`，按 `mode` 输出三种文档：
  - **学生卷（student）**：卷头含学校/班级/姓名/学号栏 + 密封线，题目不含任何答案与提示；
  - **答案卷（answer）**：仅输出「题号 + 答案」（选择题附选项标注），供教师阅卷；
  - **含答案试卷（full）**：保留题目 + 参考答案/正确选项（原行为）。
- UI 导出按钮改为「导出学生卷 / 导出答案卷 / 导出JSON（含答案）」，`initExam` 分别接线对应 mode；文件名带「学生卷/答案卷/试卷」后缀。
- 排版规范化：卷头总分、大题标题（第X大题…）、题号分值、密封线样式；导出格式为 Word 兼容 `.doc`（HTML 命名），非严格 OOXML `.docx`（如需真 `.docx` 可后续引入 docx 库）。
- 校验：`_p2_test.js` 新增第 6 节，断言学生卷含密封线/学校栏且无答案、答案卷仅含答案、三模式输出互不相同——**全部通过**。

## 八、涉及文件

- 改动：`js/modules/exam.js`
- 新增校验：`_p2_test.js`
- 文档：`试卷中心改造方案.md`（验收清单 ✅ + 第十节 P2 记录）、本报告

## 九、UI 接线验证与体验优化（2026-08-04 补充）

> 用户指令："先验证 UI 并优化一下"。因沙箱无浏览器 / jsdom，采用「UI 接线契约校验」而非真实点击。

### 1. 验证方式与结论
- 方式：VM 实跑 `renderExam()` / `renderQuestions()` 取 HTML 字符串，与 `initExam` 依赖的全部 id / class 逐一对照（无需真实 DOM）。脚本 `_p2_ui_static.js`。
- 配置页 id 全部存在：`examText / examGradeFilter / examUnitFilter / examDiffFilter / paperSections / paperTotalCount / paperTotalScore / addSectionBtn / clozeModeRow / genExamBtn / examSelCount / examResult`；
- 结果页 id 全部存在：`exportStudentBtn / exportAnswerBtn / exportJsonBtn`；
- 结构行 class 全部存在：`secType / secCount / secScore / secDel`；`clozeMode` 单选组存在。
- 结论：`renderExam` / `initExam` / `renderQuestions` 元素一致，DOM 接线逻辑自洽，**0 缺失**。
- 过程中修掉一处测试假阳性：原用精确 `class="secDel"` 匹配，实际输出为 `class="btn secDel"`，改用正则匹配 class 属性值中包含 `secDel`。

### 2. 体验优化（本次新增，均在 `js/modules/exam.js`）
1. **年级 / 难度联动防空**：难度由年级派生（高一=基础 / 高二=提高），原两个下拉各自独立——若同时选「高二 + 基础」会因 AND 组合导致课文列表为空（陷阱）。现改联动：选年级自动带出对应难度，选难度自动带出对应年级，二选一即可，杜绝空列表；并在难度筛选下加说明。
2. **已选篇数提示**：配置页组卷按钮旁新增「已选 N 篇」实时计数（筛选 / 勾选即更新）。
3. **题型题量不足透明化**：生成结果页——卷头在「实 < 应」时显示黄色「⚠️ 部分题型题量不足…（实 X / 应 Y）」；每个大题标题在实出题数小于请求数时追加「（实出 X / 应 Y 题）」；整题 0 产出仍保留原黄色提示。
4. **导出文件名带结构摘要**：新增 `paperSummary(paper)` 生成如「选10默6赏2」的结构串，学生卷 / 答案卷 / 含答案试卷 / JSON 文件名均带该摘要，便于多份卷子区分（旧扁平数组路径回退为不带摘要）。
5. **初始合计防闪**：试卷结构默认 18 题 / 80 分的合计数字改为由默认值实时计算注入，避免 JS 未执行时的硬编码与重算不一致。

### 3. 回归
- `node --check` 通过；重跑 `_p2_test.js`（P2 逻辑）、`_p1_test.js`（P1 回归 **151 题 0 异常**）、`_p2_ui_static.js`（契约 0 缺失）全绿；新增 `_p2_ui_opt_check.js` 验证上述 1–4 项逻辑（题量不足提示 / `paperSummary` / 学生卷密封线）全部通过。

### 4. 涉及文件（补充）
- 新增校验：`_p2_ui_static.js`、`_p2_ui_opt_check.js`

## 十、配置页布局重构（2026-08-04 再次补充）

> 用户反馈：截图显示「整个界面布局看着不舒服、问题很大」，要求继续优化。

### 主要问题
- 筛选器孤立在右侧窄栏，左列被课文选择 + 试卷结构上下堆叠，视觉重心左重右轻；
- 结构行横向铺开、标签与输入框未严格对齐，删除按钮红色大按钮视觉干扰强；
- 各模块缺少清晰分区，整体像「随手堆出来」。

### 重构方案
重写 `renderExam()` + `sectionRowHtml()`，改为**两栏卡片式布局**：
- **左列**：横向三列筛选栏（年级 / 单元 / 难度）+ 课文多选（标题旁显示已选篇数）+ 提示语；
- **右列**：独立浅灰卡片承载「试卷结构」，每个题型为一张白底卡片（顶部题型下拉 + 弱化删除按钮，底部题量/分值对齐），内部包含默写模式行、添加题型 / 全卷合计、以及宽幅「智能组卷」主按钮；
- **下方**：结果区横跨整宽。

### 具体改动
1. `sectionRowHtml`：从横向 flex 行改为**白底圆角卡片**，删除按钮改为透明红字「x 删除」，题量/分值用独立 label+input 组并对齐；
2. `renderExam`：外层从 `flex-wrap` 改为 **CSS Grid `repeat(auto-fit, minmax(320px, 1fr))`**，窄屏自动叠成一列；
3. 筛选器改为横向三列，难度说明缩成一行小字；
4. 「已选 N 篇」移到课文列表标题右侧，更贴近操作对象；
5. 「智能组卷」主按钮移到右列底部并占满列宽，形成清晰的最终行动点；
6. 结果区与配置区用更大间距分隔。

### 回归
- `node --check js/modules/exam.js` 通过；
- `_p2_ui_static.js` 接线契约 0 缺失（所有 id / class 仍在正确位置）；
- `_p2_test.js`、`_p1_test.js` 全绿。

## 十、课文选择改为按钮网格（2026-08-04 再次补充）

> 用户反馈：截图显示左侧课文选择区下方空旷，要求把下拉滑动选择换成按钮点击选择，占满左列。

### 改动
- `renderExam`：`<select id="examText" multiple>` 改为 `<div id="examText" class="text-chip-grid">`，内部渲染 137 个 `.text-chip` 按钮；
- 选中态：主色背景 + 白字 + 主色边框；未选中：白底灰边；`min-height:180px` + `align-content:start` 让网格占满左列；
- `initExam`：`applyTextFilter` 渲染 chip 时保留已选状态；`updateSelCount` 从 `.text-chip.selected` 计数；点击事件改为 chip 容器委托切换 `selected` 类并同步内联样式；`genExamBtn` 收集已选从 chip 读取；
- 文案从「可按住 Ctrl 多选」改为「点击选择，可反复点击切换」；未选课文时 alert 同步调整。

### 回归
- `node --check js/modules/exam.js` 通过；
- `_p2_ui_static.js` 通过，报告课文 chip 数量 137；
- `_p2_test.js`、`_p1_test.js`（151 题 0 异常）全绿。

### 涉及文件（补充）
- 改动：`js/modules/exam.js`
- 校验更新：`_p2_ui_static.js`

## 十一、课文选择改为横向滑动 + 左右栏底部对齐（2026-08-04 再次补充）

> 用户反馈：左侧课文按钮网格铺得太开（红框处），整体底部不齐，要求改为横向滑动并加左右按钮。

### 改动
- `renderExam`：
  - 课文选择区从 `text-chip-grid`（网格铺满）改为 `text-chip-scroll`（横向单行滚动容器），超出部分可滑动；
  - 滚动条底部增加「← 左滑 / 右滑 →」按钮；
  - 左右两列外层 grid 改为 `align-items:stretch`，左列与右列均使用 `display:flex;flex-direction:column`，提示语/主按钮分别用 `margin-top:auto` 贴底，实现左右底部对齐。
- `applyTextFilter`：筛选后重新渲染的 chip 同步为横向滚动条样式。
- `initExam`：为左右滑动按钮绑定 `scrollBy({ left: ±200, behavior:'smooth' })`。

### 回归
- `node --check js/modules/exam.js` 通过；
- `_p2_ui_static.js` 通过，报告课文 chip 数量 137；
- `_p2_test.js`、`_p1_test.js` 全绿。

### 涉及文件（补充）
- 改动：`js/modules/exam.js`
- 校验更新：`_p2_ui_static.js`

## 十二、课文选择区占满高度 + 隐藏底部结果区（2026-08-04 再次补充）

> 用户反馈：左侧课文选择区（黄框）只显示一行、下方空白；底部结果提示区（红框）要去掉。

### 改动
- `renderExam`：
  - 课文选择区外包裹改为 `flex:1;min-height:0`，占满左列剩余高度；
  - `#examText` 从单行横向滚动改为**多列横向滑动网格**：`display:grid; grid-auto-flow:column; grid-auto-columns:minmax(100px,120px); grid-template-rows:repeat(auto-fill, minmax(38px,1fr)); overflow-x:auto;`，chip 先纵向填满一列，再向右新增列，实现"占满"与"横向滑动"同时满足；
  - 底部保留「← 左滑 / 右滑 →」按钮；
  - `#examResult` 初始 `display:none`，点击「智能组卷」后显示，去掉红框提示区。
- `applyTextFilter`：筛选后重新渲染的 chip 同步为多列横向滑动样式。

### 回归
- `node --check js/modules/exam.js` 通过；
- `_p2_ui_static.js` 通过；
- `_p2_test.js`、`_p1_test.js` 全绿。

### 涉及文件（补充）
- 改动：`js/modules/exam.js`
- 校验更新：`_p2_ui_static.js`

## 十三、P2 收尾两块：保存/历史 + 单题微调（2026-08-04）

> 用户指定落地 P2 剩余两块：① 保存/命名/历史（localStorage 持久化、可复用二次编辑）；② 生成后单题微调（删题 / 调序 / 替换 / 收藏）。

### A. 保存 / 命名 / 历史（localStorage 持久化）
- 新增模块级存储函数：`getPaperStore / setPaperStore / savePaperToStore / renderHistoryInto / loadSavedPaper / deleteSavedPaper`，键 `teacher_exam_papers_v1`。
- 结果页新增保存工具条：命名输入框 `paperNameInput` + 「💾 保存试卷」+「📋 另存副本」+「★ 收藏夹(N)」。
- `savePaperToStore`：首次保存生成 `rec._id` 并写回 `paper._id`；带 `_id` 同名保存为**更新**（不新增）；另存副本（`asCopy=true`）始终新增一条；保存内容含 `_srcTexts / _clozeMode`（供「替换」二次生成）。
- `renderExam` 新增 `#paperHistory` 历史面板，初始渲染；每条显示名称 / 结构摘要 / 题数·总分·保存日期，含「打开」（载入 resultDiv 二次编辑）与「删除」。
- 保存成功 / 删除均弹轻量 toast 提示。

### B. 生成后单题微调
- 每题底部加操作条：`🗑 删除` / `↑ 上移` / `↓ 下移` / `🔄 替换` / `☆ 收藏`（选中态高亮为 ★ 已收藏）。
- 模块级纯函数（便于校验）：`ensurePaperUids / findQInPaper / recomputePaper / deleteQuestionInPaper / moveQuestionInPaper / replaceQuestionInPaper / insertFavIntoPaper / toggleFav`。
- 生成时每题写入稳定 `_uid`（`generateMixedPaper` 内）；删除/调序/替换后 `recomputePaper` 自动重算各节标题、总分、总题数。
- **替换**：用 `paper._srcTexts / paper._clozeMode` 同题型重新生成（优先选与现有不重复的题目），无原文信息时安全失败并提示。
- **收藏**：`toggleFav` 写入 `teacher_exam_favs_v1`，「★ 收藏夹(N)」展开列表可「＋ 插入当前试卷」（按题型并入对应大题，无则新建大题）或「移出」。
- `initExam` 改为对 `#examResult` 做**事件委托**，统一处理 导出 / 保存 / 微调 / 收藏 / 历史 全部按钮（导出也走委托，避免微调重渲染后事件丢失）。

### 回归
- `node --check js/modules/exam.js` 通过；
- `_p2_ui_static.js` 通过（新增结果页 id：paperNameInput / savePaperBtn / clonePaperBtn / favToggleBtn / favPanel 与单题 class：q-del / q-up / q-down / q-replace / q-fav，配置页 paperHistory）；
- 新增 `_p2_tune_test.js`：_uid 唯一性、删题+重算、调序越界、替换、收藏 toggle、历史保存/更新/副本/删除全通过；
- `_p2_test.js`、`_p1_test.js` 全绿。

### 涉及文件（补充）
- 改动：`js/modules/exam.js`
- 新增校验：`_p2_tune_test.js`
- 校验更新：`_p2_ui_static.js`

## 十四、历史卷导出 + 收藏夹批量管理（2026-08-04 收尾补充）

> 用户追加：导出历史卷、收藏夹批量管理。

### A. 导出历史卷
- `renderHistoryInto` 每条历史记录新增 **Word**（导出学生卷）/ **JSON**（含答案，可二次编辑）按钮，class：`hist-word` / `hist-json`，data-id 同 `_id`。
- `initExam` 委托新增 `hist-word` → `exportToWord(rec, {mode:'student'})`、`hist-json` → `exportToJson(rec)`，`rec` 直接取自 store，无需先打开。

### B. 收藏夹批量管理
- `renderFavPanelInto` 顶部工具条新增 **＋ 全部插入当前试卷**（`fav-insert-all`）与 **🗑 清空收藏夹**（`fav-clear`）。
- `initExam` 委托新增：
  - `fav-insert-all`：遍历全部收藏 `insertFavIntoPaper` 进当前试卷（按题型并入对应大题），统一 `afterTune` + toast；
  - `fav-clear`：`setFavs([])` 清空，重渲染收藏面板 + `afterTune`。
- 抽 `ensureCurrentPaper()`：单条/批量「插入试卷」时若 `currentPaper` 为空（尚未组卷），自动建空卷并展示到 `#examResult`，避免插入到 null 崩溃。

### 回归
- `node --check js/modules/exam.js` 通过；
- `_p2_ui_static.js` 扩展：mock localStorage 注入假历史/收藏，断言历史面板 `hist-load/hist-word/hist-json/hist-del` 与收藏面板 `fav-insert/fav-remove/fav-insert-all/fav-clear` 全部存在；
- `_p2_tune_test.js`、`_p2_test.js`、`_p1_test.js` 全绿。

### 涉及文件（补充）
- 改动：`js/modules/exam.js`
- 校验更新：`_p2_ui_static.js`
