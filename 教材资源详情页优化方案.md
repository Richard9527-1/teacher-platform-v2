# 语文智备Pro · 教材资源详情页优化方案

> 基于对项目结构、现有数据与功能的梳理，为「教材资源 → 文章详情」提供可落地的升级方案。

---

## 一、项目现状速览

- **技术栈**：纯原生 HTML/CSS/JS，无框架、无构建，数据放在 `data/sample-data.js`。
- **教材资源入口**：侧边栏「📁 教材资源」，包含 132 篇统编版高中语文篇目（课文 / 文言文 / 古诗词）。
- **当前详情页**：点击「查看」后弹出一个模态框，仅展示：
  - 标题、作者、朝代、年级、单元、类型
  - 背诵要求
  - `original` 原文（`max-height:200px` 截断滚动，且数据中大量为节选）
  - 标签、重点、考点
- **现有短板**：无全文展示、无生僻字拼音、无重难点句子讲解、无注释翻译、无赏析、无朗读入口。

---

## 二、优化目标

让文章详情页从「信息卡片」升级为「备课级学习页」，核心补齐三块能力：

1. **全文呈现**：完整展示课文正文，支持分段、分章节、背诵标记。
2. **生僻字拼音标注**：对原文中较生僻的字词自动/手动标注拼音，鼠标悬停可查看。
3. **重难点句子讲解**：逐句或逐段给出翻译、赏析、写作手法、考点提示。

同时兼顾备课场景，增加朗读、加入备课、导出讲义等快捷操作。

---

## 三、功能设计方案

### 3.1 详情页结构（标签页 Tabs）

建议把当前单页弹窗改为侧边滑出面板或更大尺寸的模态窗，顶部用标签页组织内容：

| 标签 | 内容 | 适用类型 |
|---|---|---|
| **全文** | 完整正文、分段、背诵段落高亮、朗读按钮 | 全部 |
| **拼音** | 原文 + 生僻字注音（可开关）、注音词表 | 文言文 / 古诗词 / 难读课文 |
| **注释** | 重点字词释义、通假字、古今异义、词类活用 | 文言文 / 古诗词 |
| **译文** | 全文翻译 / 重点句翻译 | 文言文 / 古诗词 |
| **讲解** | 重难点句子逐句赏析、写作手法、考点提示 | 全部 |
| **赏析** | 主题思想、结构分析、艺术特色、名句赏析 | 全部 |
| **考点** | 默写名句、常见题型、真题链接 | 全部 |

### 3.2 全文页

- **完整正文**：用 `fullText` 字段替换当前节选 `original`，按段落/章节渲染。
- **背诵标记**：对要求「全文背诵」的篇目，给全文加浅色高亮背景；对局部背诵篇目，可给对应段落加 🌟 标记。
- **朗读按钮**：复用 app.js 中已有的 TTS 能力，点击后语音朗读全文。
- **字号调节**：提供「小 / 中 / 大」三档字号，方便课堂投影。
- **复制原文**：一键复制全文到剪贴板。

### 3.3 生僻字拼音标注

- **自动标注**：
  - 内置一个常见生僻字拼音表（如：「輮 róu」「槁暴 gǎo pù」「踟蹰 chí chú」）。
  - 对原文进行分词/逐字匹配，命中生僻字时在其上方显示拼音（`ruby` 标签或 CSS 实现）。
  - 提供「显示拼音 / 隐藏拼音」开关，避免干扰熟练学生。
- **手动补充**：
  - 在数据结构中为每篇课文维护 `pinyinMap`：{ 字词: 拼音 }，便于语文老师校准。
  - 对古诗词/文言文中的异读字（如「说 yuè」「女 rǔ」）特别标注。
- **拼音词表**：在拼音标签页底部列出本篇所有生僻字词表，方便快速复习。

### 3.4 重难点句子讲解

- **逐句讲解**：
  - 数据结构中新增 `hardSentences` 数组，每项包含：原句、翻译、讲解、考点标签。
  - 页面中以「句子卡片」形式展示，原文在左/上，讲解在右/下。
- **讲解维度**：
  - **翻译**：文言文/古诗词给出白话翻译。
  - **赏析**：修辞手法、炼字、意象、情感。
  - **考点**：该句常见考题、易错点、默写提示。
- **内联标注**：在全文页中，对重难点句子加下划线或高亮，鼠标悬停显示简要提示，点击跳转到讲解页对应卡片。

### 3.5 其他配套能力

- **注释页**：复用 `js/modules/wenyan.js` 已沉淀的文言知识点结构（通假字、古今异义、词类活用、特殊句式、重点字词），把当前「文言文神器」中的数据与教材资源打通。
- **赏析页**：主题思想、结构思路、艺术特色、名句赏析。
- **考点页**：名句默写、常见题型、答题模板。
- **加入备课**：保留现有功能，加入备课时可选择把「全文 / 注释 / 讲解」一并带入。
- **导出讲义**：支持导出当前篇目的 Markdown / Word / 打印友好页面。

---

## 四、数据模型改造方案

当前单篇数据结构较简单，建议向后兼容扩展，新增可选字段：

```js
{
  // 已有字段
  grade, unit, type, author, dynasty,
  original,        // 可继续保留作为摘要/节选
  tags, keyPoints, examPoints, 背诵要求,

  // 新增字段
  fullText: `完整课文正文，按段落用\n分隔...`,

  // 生僻字拼音表
  pinyinMap: {
    '輮': 'róu',
    '槁暴': 'gǎo pù',
    '踟蹰': 'chí chú'
  },

  // 注释（字词 -> 释义）
  notes: {
    '輮': '使……弯曲（通"煣"）',
    '中绳': '合乎墨线'
  },

  // 重难点句子讲解
  hardSentences: [
    {
      sentence: '青，取之于蓝，而青于蓝；冰，水为之，而寒于水。',
      translation: '靛青是从蓝草中提取的，但比蓝草颜色更深；冰是由水凝结而成，但比水更寒冷。',
      explanation: '以比喻论证说明学习可以提升人、改变人，强调后天学习的重要性。',
      tags: ['比喻论证', '名句默写', '劝学主旨']
    }
  ],

  // 文言知识点（复用 wenyan.js 结构）
  wenyan: {
    tongjia: ['輮 同 煣'],
    gjyi: ['跪：古义指脚，今义跪下'],
    cihuolei: ['水：名词作动词，游泳'],
    specialSentence: ['蚓无爪牙之利，筋骨之强（定语后置）'],
    keyWords: ['而', '于', '之']
  },

  // 赏析
  appreciation: {
    theme: '通过比喻和对比，阐述学习不可停止的道理。',
    structure: '开篇立论 → 比喻论证 → 正反对比 → 得出结论。',
    features: ['比喻贴切', '对比鲜明', '句式整齐'],
    famousLines: ['青，取之于蓝，而青于蓝']
  },

  // 考点
  examFocus: {
    dictation: ['青，取之于蓝，而青于蓝', '君子博学而日参省乎己'],
    questionTypes: ['论证方法', '名句默写', '文言实词']
  }
}
```

### 数据补充策略

1. **全文补齐**：`fullText` 是核心前提。可分批从教材/权威来源补齐 132 篇全文。
2. **拼音/注释/讲解**：
   - 优先补齐高考高频篇目（如《劝学》《赤壁赋》《离骚》《蜀道难》等）。
   - 可借助外部资源/工具批量生成初稿，再由语文老师校准。
3. **向后兼容**：代码中对新字段做防御性读取，旧数据缺失时降级显示 `original`，不报错。

---

## 五、UI/UX 改造方案

### 5.1 页面形式

- **推荐**：右侧滑出抽屉（`right drawer`），宽度 `min(900px, 90vw)`，避免弹窗太小。
- **备选**：加大版居中模态窗（`max-width:900px; max-height:90vh`）。
- 顶部固定：标题、作者/朝代/年级/单元、操作按钮（朗读、加入备课、导出）。
- 下方标签页切换内容区。

### 5.2 样式要点

- 全文区使用大字号、宽行距（`line-height: 1.8~2`），适合阅读/投影。
- 拼音使用 `<ruby><rt>` 或自定义 CSS 实现，保持与原文对齐。
- 重难点句子讲解使用卡片布局，原文用引用样式，讲解分点列出。
- 暗色主题下所有新增区域需适配 `var(--card-bg)` / `var(--bg)` / `var(--text)`。

### 5.3 新增/调整文件

| 文件 | 改动 |
|---|---|
| `js/modules/resource.js` | 重写 `showDetailModal` 为标签页详情组件，新增拼音渲染、重难点句子渲染函数 |
| `js/modules/resource-detail.js`（可选拆分） | 把详情页逻辑独立，便于维护 |
| `data/sample-data.js` | 逐步补齐 `fullText`、`pinyinMap`、`notes`、`hardSentences` 等字段 |
| `css/style.css` | 新增详情页标签页、拼音、句子卡片、抽屉动画等样式 |
| `js/utils.js` | 新增拼音匹配、文本高亮、HTML 转义等通用工具函数 |

---

## 六、实现步骤与优先级

### 第一阶段：框架改造（1~2 天）

1. 改造 `showDetailModal`，改为大模态窗/抽屉 + 标签页结构。
2. 新增「全文」标签页，读取 `fullText` 完整展示，支持字号调节、朗读。
3. 新增通用工具函数：HTML 转义、拼音标注渲染、文本分段。
4. 对缺失 `fullText` 的篇目，先以 `original` 兼容展示，并在顶部提示「全文待补充」。

### 第二阶段：拼音标注（2~3 天）

1. 建立通用生僻字拼音表（可先从高中常见文言实词/异读字入手）。
2. 在数据模型中加入 `pinyinMap`，支持篇目级校准。
3. 实现「拼音」标签页 + 全文页的拼音开关。
4. 先补齐 10~20 篇高频文言文/古诗词的拼音数据做验证。

### 第三阶段：重难点句子讲解（3~5 天）

1. 设计 `hardSentences` 数据结构。
2. 实现「讲解」标签页：句子卡片 + 翻译/赏析/考点。
3. 在全文页中对重难点句子做高亮，点击联动到讲解卡片。
4. 补齐高频篇目的讲解数据。

### 第四阶段：注释、译文、赏析、考点（持续补充）

1. 打通 `wenyan.js` 的文言知识点数据，迁移到 `sample-data.js` 的 `wenyan` 字段。
2. 新增「注释」「译文」「赏析」「考点」标签页。
3. 建立数据补充流程，可邀请老师分批录入。

### 第五阶段：导出与备课联动（可选）

1. 支持导出当前篇目为 Markdown / 打印页。
2. 加入备课时可选择携带「原文 + 拼音 + 讲解」。

---

## 七、关键风险与建议

1. **数据量是最大投入**：132 篇课文的全文、拼音、讲解需要大量人工校对，建议：
   - 优先做高考高频篇目，不要求一次性补齐全部。
   - 可以先用通用拼音表 + 通用讲解模板做兜底，再逐步人工精修。
2. **拼音准确性**：中文多音字多，自动注音会出错，必须保留 `pinyinMap` 让老师手动修正。
3. **版权问题**：补齐全文时建议使用教材原文或公有领域文本，避免版权风险。
4. **性能**：全文较长时，拼音标注和句子卡片渲染可能影响性能。可对大文本做虚拟滚动或分段懒加载。
5. **移动端**：当前为桌面端为主，抽屉宽度需做响应式适配。

---

## 八、最小可行版本（MVP）建议

如果希望快速看到效果，可以先做：

1. 把详情页改为带标签页的大抽屉。
2. 新增「全文」标签页，展示 `fullText`（先手动补齐几篇示例全文）。
3. 新增「拼音」标签页，用通用生僻字表对全文做自动标注。
4. 新增「讲解」标签页，手动录入 5~10 篇高频篇目的 `hardSentences`。

这样即可在保留现有功能的基础上，立刻提升教材资源详情页的教学价值。

---

## 九、后续可拓展方向

- 与「备课中心」打通：一键生成包含原文、拼音、讲解的教案 PPT。
- 与「作文批改 / OCR」打通：扫描学生默写卷后，自动对照课文原文批改。
- 与「试卷中心」打通：从篇目考点自动生成名句默写题、翻译题。
- AI 辅助：接入大模型，根据原文自动生成拼音、注释、讲解（需人工审核）。
