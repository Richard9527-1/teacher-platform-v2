# 数据分析模块 · 优化方案

> 适用范围：`js/modules/data-analysis.js`（数据分析中心）
> 关联数据层：`js/modules/class-data.js`（班级/学生/考勤/考试/成绩）
> 配套模块：成绩榜 `scoreboard.js`、工作台 `dashboard.js`、考勤历史（`class.js`）
> 生成日期：2026-08-06

---

## 一、现状梳理

### 1.1 现有功能清单（6 个子视图）

| 按钮 | 函数 | 能力 |
|------|------|------|
| 📝 录入成绩 | `renderExamEntry` / `renderScoreEntry` / `bindScoreEntryEvents` | 创建考试（名称/类型/班级/日期），逐生录入 总分·默写·阅读·作文·备注 |
| 📋 成绩列表 | `renderExamList` / `bindExamListEvents` | 考试一览、查看明细、删除（连成绩一并删） |
| 📈 学情分析 | `renderAnalysis` | 逐生平均分 + 最弱题型（默写/阅读/作文取最低） |
| 📉 趋势图 | `renderTrend` / `drawTrendChart` | 原生 Canvas 手绘前 10 名学生总分折线 |
| 📊 综合报表 | `renderReport` | 逐生：考试次数 / 平均分 / 缺课 / 迟到 / 请假 次数 |
| ⚠️ 预警管理 | `renderAlert` | 成绩下滑（近两次差 < -10）、缺课累计 ≥ 3 |

### 1.2 数据模型（已确认）

- 班级/学生：`classData.classes`、`classData.students`
- 考试：`examData.exams = {id, name, type, classId, date, createdAt}`
- 成绩：`examData.scores = {id, examId, studentId, chinese(=总分), dictation, reading, writing, note, createdAt}`
- 考勤：`attendanceData = {attendance:[{studentId,date,type:'缺课'|'迟到'}], leaves:[{studentId,date,type,status}]}`

### 1.3 已有但未利用的资产

- **Chart.js v4 UMD 已在 `index.html` 第 74 行本地内置**（离线可用），但 `data-analysis.js` 与 `scoreboard.js` 的趋势图均用原生 Canvas 手绘，完全没用上。
- 全局 Excel 导入/导出（`exportClassExcel`/`parseClassWorkbook`）已成熟，可复用为分析结果导出底座。

---

## 二、问题诊断（按严重程度）

### 🔴 严重（功能缺陷 / 数据错误）
1. **趋势图 X 轴错位 Bug**：`drawTrendChart` 用 `scores.forEach((sc,i)` 的 `i/(scores.length-1)` 定位 X 坐标，而 X 轴标签用的是全部 `examNames`。一旦某学生缺考某场，其点数少于考试总数，折线点会与真实考试日期错位，呈现"假曲线"。
2. **"连续缺课"语义错误**：`renderAlert` 中 `attAbsent.length >= 3` 实际统计的是**累计缺课次数**，却被标注为"连续缺课"，与教师认知不符。
3. **分数线硬编码 150**：`pad + (1 - (chinese||0)/150) * ...` 把满分写死 150，若试卷满分 100/120 会被压扁到图表底部，毫无参考性。

### 🟠 中等（体验与价值缺失）
4. **图表零交互**：手绘 Canvas 无 hover tooltip、无图例点击、无网格线、无 Y 轴刻度；10 条线以上颜色循环重复难分辨；X 轴考试名重叠。
5. **分析维度单一**：只有"个人均值 + 最弱项"，**缺班级/年级对比、缺分数段分布、缺题型得分率、缺优秀率/及格率/离散度**，对教学决策帮助有限。
6. **首页无 KPI 看板**：进入模块默认就是一张大表，没有"最高分/班级均分/及格率/预警人数/出勤率"等一眼可见的概览。
7. **预警体系单薄**：仅 2 类（下滑、缺课），无低分预警、偏科预警、临界生（升降趋势）、进步表扬、长期请假。阈值写死不可配。
8. **成绩录入无校验**：总分与各题型之和不校验、分数范围（0~满分）不校验、不能从已有考试复制模板、不支持批量导入成绩。

### 🟡 轻微（工程与一致性）
9. **重复绘图代码**：`drawTrendChart` 与 `scoreboard.js` 的 Canvas 绘图高度雷同，应抽公共 chart helper。
10. **字段语义混乱**：成绩里 `chinese` 实为"总分"，命名误导后续维护。
11. **事件绑定碎片化**：`bindExamEntryEvents` 的返回逻辑（`showView` 回综合报表 vs 占位）与 `showView` 不一致；`renderDataAnalysis` 与 `initDataAnalysis` 尾部对 `renderReport` 双重渲染。
12. **趋势图固定 top 10**：`students.slice(0,10)` 不可选，班级筛选后仍是全局前 10，不按筛选结果排序/自选。
13. **分析结果不可导出**：报表/学情/预警纯展示，无法导出 Excel/PDF/图片，教师无法留档或上报。

---

## 三、优化方案

### 🎯 P0 —— 核心体验（投入小、回报大，建议先做）

#### P0-1 图表统一迁移到 Chart.js，并修复趋势 Bug
- **做法**：删除 `drawTrendChart` 原生 Canvas 实现，改用 `new Chart(ctx, {type:'line', ...})`。Y 轴 `suggestedMax` 取该次/该班考试满分（从考试配置或成绩最大值推导），不再硬编码 150。
- **修复错位**：折线数据点必须按**真实考试时间戳**对齐 X 轴（用 `labels = 考试日期升序`，每个学生只在其参加过的考试处放点，`spanGaps:false`），缺考自动断点。
- **收益**：获得 hover tooltip、点击图例隐藏、响应式缩放、动画、网格线；同时消灭 3 个严重 Bug（错位/语义/150）。
- **涉及**：`renderTrend`、`drawTrendChart`（重写）、`index.html`（Chart.js 已就位，无需改动）。

#### P0-2 模块首页增加 KPI 概览看板
- **做法**：在 `renderDataAnalysis` 顶部（综合报表之上）加一行卡片：本学期最高分、班级平均分、及格率、预警人数、出勤率（出勤率 = 1 − 异常人次/应到人次）。
- **收益**：教师进入即见全局健康度，无需先读懂大表。
- **涉及**：`renderDataAnalysis` 新增 `renderKpiBar()`。

#### P0-3 预警体系升级 + 阈值可配
- **做法**：
  - 修正"连续缺课"为**真正连续**（按日期排序后判断相邻缺课是否为连续天），或改名"累计缺课"并明确口径；
  - 新增：低分预警（个人平均分 < 班级均分 − N）、偏科预警（某题型低于班级该题型均值 − N）、进步表扬（近两次 ↑≥10）、长期请假（请假 ≥ X 天）；
  - 阈值（N、X）放入 `settings` 或模块内"预警设置"抽屉，存 localStorage。
- **收益**：把"报警"变成"可行动的教学信号"，契合语文教师实际关切。
- **涉及**：`renderAlert`、`class-data.js`（读设置）。

### 🎯 P1 —— 分析深度（决策支撑）

#### P1-1 班级 / 年级对比柱状图
- 班级平均分横向对比 + 与年级均值参考线；支持按考试类型聚合。

#### P1-2 分数段分布直方图
- 每场考试按 0–59 / 60–69 / 70–79 / 80–89 / 90–100 / 100+ 分段计数（满分自适应），直观看成绩集中度。

#### P1-3 题型得分率雷达图
- 个人 vs 班级：默写 / 阅读 / 作文 / 总分 的得分率（实际/满分）雷达对比，定位薄弱题型。

#### P1-4 报表增强
- 综合报表增加**汇总行**（班级均分、及格率、最高/最低分）、列支持点击排序；学情分析增加"与班级均值差"列。

> 以上图表均建立在 P0-1 的 Chart.js helper 之上，边际成本低。

### 🎯 P2 —— 效率与打通

#### P2-1 成绩录入校验与提效
- 总分 = 默写 + 阅读 + 作文 的自动校验提示；单科分数 0~满分边界校验；"从已有考试复制名单/模板"；支持**批量导入成绩**（复用 `parseClassWorkbook` 的成绩 Sheet）。

#### P2-2 趋势图交互筛选
- 支持勾选学生、按题型维度（总分/默写/阅读/作文）切换、班级筛选后结果重排而非固定 top10。

#### P2-3 分析结果导出
- 报表/学情一键导出 Excel（沿用 `exportClassExcel` 思路单独生成分析结果 Sheet），趋势图/分布图导出 PNG（`chart.toBase64Image()`）。

#### P2-4 与教材资源联动（增值）
- 利用教材详情页已有的「考点 / 赏析」结构化数据，做"知识点掌握度"——按考点命中率（该考点相关题得分）评估班级薄弱知识点；背诵默写完成率与成绩"默写"分联动展示。

#### P2-5 代码重构
- 抽 `chartHelper.js` 共用（`data-analysis` + `scoreboard`）；成绩字段语义化（`chinese → totalScore`，需写迁移）；消除大量内联 style（抽到 `css/style.css` 的 `.da-*` 类）；统一事件绑定到 `showView` 体系。

---

## 四、实施路线（建议里程碑）

| 阶段 | 内容 | 产出 |
|------|------|------|
| M1（P0） | 图表迁移 Chart.js + 修 3 个严重 Bug + KPI 看板 + 预警升级 | 趋势正确、首页有概览、预警可配 |
| M2（P1） | 对比柱状 / 分数段直方 / 题型雷达 / 报表汇总排序 | 形成"教学决策级"分析 |
| M3（P2） | 录入校验+批量导入、趋势筛选、导出、教材联动、重构 | 效率与生态打通 |

---

## 五、预期效果与风险

- **预期**：数据分析从"罗列数字"升级为"一眼看全局 + 可下钻定位薄弱点与薄弱生"，直接服务语文教师的培优补差决策。
- **风险与建议**：
  - `chinese → totalScore` 改名属破坏性变更，须写一次性迁移（`loadExams` 内兼容旧字段），否则老用户成绩丢失。
  - Chart.js 已本地内置，离线可用；勿改为 CDN，以免破坏 GitHub Pages 离线能力。
  - 雷达/分布图依赖样本量，空数据需保持现有"暂无数据"友好态。

---

## 附：改动文件清单（预估）

- `js/modules/data-analysis.js`（重写趋势、扩充预警/报表/KPI）
- `js/modules/class-data.js`（读预警设置、可选字段迁移）
- `css/style.css`（新增 `.da-*` 样式、暗色适配）
- `js/lib/chart.umd.js`（已存在，无需改）
- 可选新增 `js/modules/chart-helper.js`（共用绘图）
- 可选 `js/modules/exam.js`（批量导入成绩入口）
