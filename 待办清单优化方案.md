# 待办清单功能 · 现状梳理与优化方案

> 目标：梳理「⏰ 待办清单」现有功能/内容，定位待优化点，给出可落地的分阶段改造方案。
> 涉及文件：`js/modules/todo.js`（核心）、`js/app.js`（首页角标 + 快速统计）、`js/modules/global-search.js`（全局搜索）、`index.html`（入口）。

---

## 一、现状梳理（功能与内容）

### 1. 入口与渲染
- 侧边栏 `index.html` 第 56 行：`<li data-module="todo">⏰ 待办清单</li>`。
- `app.js` 的 `showModule('todo')` → `main.innerHTML = renderTodo()` → `initTodo()` 绑定「＋添加待办」按钮。

### 2. 数据存储
- 键名：`localStorage['todoItems']`，存一个**数组**。
- 单条结构（创建时写入）：
  ```
  { text, source, time:'HH:MM'|'', done:false, date:'YYYY-MM-DD'(=创建日), createdAt:Date.now() }
  ```
- 读取：`loadTodoItems()` = 取全量后 `filter(item.date === 今天)`。
- 保存：`saveTodoItems(items)` = 直接覆盖写回传入数组。

### 3. 界面与交互
- 顶部统计卡：`总任务 / 已完成 / 未完成` + 「＋添加待办」按钮。
- 列表项：勾选框（切换完成）、内容、来源·时间、🗑️ 删除（confirm 确认）。
- 添加：弹窗表单，字段 = 事项内容 / 来源关联 / 时间（可选）。
- 完成态：透明度 0.6 + 删除线。

### 4. 跨模块联动
- **首页四角标**「⏰ 今日待办」：`app.js` `getTodayTaskStats()` 显示「今日 done/total · 逾期 N」，点击跳 `todo` 模块。
- **快速统计弹窗** `openQuickStats()`：按 `dueDate||date` 统计今日完成。
- **全局搜索** `global-search.js`：覆盖 `todoItems`，按 `text` 模糊匹配，结果点击跳 `todo` 模块。

---

## 二、待优化点（按严重程度）

### P0 · 严重（数据安全 / 口径冲突）
1. **数据丢失 Bug（必修）**：`loadTodoItems()` 只取"今日"，而 `saveTodoItems()` 又把"今日子集"**整体覆盖**写回。每次新增/勾选/删除，都会把非今日待办从 `localStorage` 抹掉。多日/逾期模型形同虚设，历史待办在任一次保存后被清空。
2. **多日/逾期口径不自洽**：`app.js` 用 `dueDate` 字段、首页显示「逾期 N」；但 `todo.js` 从未写 `dueDate`、也只产生"今日"项；`getTodoStats()` 函数**根本未定义**（`typeof getTodoStats==='function'` 永远 false）。结果：首页"逾期"永远为 0，两套字段（`date` vs `dueDate`）互相打架。
3. **XSS 风险**：`item.text` / `item.source` 未经转义直接拼进 `innerHTML`，教师若输入 `<img src=x onerror=...>` 之类内容可执行脚本。应统一 `escapeHtml`。

### P1 · 体验（能力缺失）
4. 无**到期日/截止时间**：只有创建日，无法排期；逾期、临近提醒无法实现。
5. 无**优先级**：高/中/低无法区分，长列表扁平无重点。
6. 无**分类/标签**：无法按"备课/批改/行政/会议"归类与筛选。
7. 无**行内编辑**：写错只能删除重加，不能改内容/时间。
8. 无**排序/置顶/已完成折叠**：长列表难管理，已完成项仍占空间。
9. 无**重复任务**：如"每周一批改周记"需手动反复建。
10. 无**与备课/课表联动派生**：待办只能手敲，未从 `lessonTasks` 逾期、当日 `schedule` 课程自动生成。

### P2 · 增强
11. 模块内**搜索/筛选**（与全局搜索呼应，但模块自身无过滤）。
12. 空态引导更友好 + 回车快捷提交。
13. 完成趋势 / 今日小结（首页已有雏形，可加强）。
14. **导入导出备份**（对齐班级 Excel 模板体系，便于换机/恢复）。

---

## 三、优化方案（分阶段）

### 阶段 1 · 止血 + 统一数据层（必修，改动小回报高）
- **修复保存逻辑**：新增 `getTodoAll()` / `saveTodoAll(arr)` / `upsertTodo(item)` / `removeTodo(id)`，所有写操作基于**全量列表 + id 定位**，绝不用"今日子集"覆盖全量。
- **统一字段口径**：弃用 `date`，全项目统一 `dueDate`（`'YYYY-MM-DD'`，默认创建日）。补 `id`（用 `createdAt+随机` 或自增）。
- **实现 `getTodoStats()`**：返回 `{ todayTotal, todayDone, overdue, allOpen }`，供首页角标与快速统计共用，消除 `dueDate`/`date` 双口径。
- **全部用户文本 `escapeHtml`** 后再进 DOM。

### 阶段 2 · 核心体验增强（P1）
- **到期日 + 逾期/临近角标**： overdue 红、当日黄、未来灰；列表顶部显示"逾期 N 项"提示。
- **优先级**：`high/normal/low`，颜色标识 + 排序权重（高→中→低→已完成）。
- **分类标签 + 筛选 Tab**：全部 / 备课 / 批改 / 行政 / 会议 / 家校 / 其他 + 关键字搜索框。
- **行内编辑**：点内容进入编辑态，或复用弹窗改内容/时间/优先级/分类。
- **拖拽排序 + 置顶 + 已完成折叠**：长列表可管理；可一键折叠已完成。

### 阶段 3 · 联动与增强（P2）
- **重复任务**：`repeat: none|daily|weekly|monthly`，跨日时自动派生下一条。
- **联动派生**：`lessonTasks` 逾期未完 → 自动进当日待办；`schedule` 当日课程 → 一键生成"上课提醒"待办（需动 `lesson-plan.js` / `schedule` 相关模块）。
- **导入导出**：JSON 与 Excel 两套，对齐班级备份体系。
- **完成趋势小图**：在待办页与首页展示近 7 日完成曲线。

---

## 四、实施优先级建议
| 优先级 | 内容 | 风险 | 建议 |
|---|---|---|---|
| 第 1 步（必修） | 修数据丢失 Bug + 统一 `dueDate` + 实现 `getTodoStats` + escapeHtml | 低 | 立即做，止血 |
| 第 2 步（核心） | 到期日/优先级/分类筛选/行内编辑/折叠排序 | 中 | 跟在第 1 步后 |
| 第 3 步（增强） | 重复任务/联动派生/导入导出/趋势 | 中高 | 视需求排期 |

---

## 五、需要你确认的点
1. **范围**：本次先只做「第 1 步止血」，还是连带「第 2 步核心体验」一起做？
2. **联动派生**：是否要做"备课逾期 / 当日课表自动生成待办"？（会牵动 `lesson-plan.js` 与课表模块）
3. **分类默认集合**：用哪套？建议 `备课 / 批改 / 行政 / 会议 / 家校 / 其他`。
4. **备份**：是否需要导入导出（对齐班级 Excel 体系）？

> 结论：当前待办清单是一个"仅当日、易丢数据、无排期无优先级"的雏形；最紧迫的是修掉保存覆盖 Bug 并统一 `dueDate` 口径，再逐步补齐排期/优先级/分类/联动能力。
