# 语文智备Pro · 软件体检与优化建议报告

> 体检日期：2026-08-04
> 范围：全部 11 个导航模块 + 登录 + 数据层 + 部署/PWA
> 结论：**内容数据健康（100% 覆盖），功能可用但有部分"名不副实/入口失效"，健壮性有安全与体验欠账。**

---

## 一、体检总览

| 维度 | 健康度 | 一句话结论 |
|---|---|---|
| 📦 内容数据 | 🟢 优秀 | 137/137 全库覆盖、字段齐全、难句连续性 0 问题、试卷生成完整 |
| ⚙️ 功能完整 | 🟡 中等 | 核心 CRUD/组卷正常，但"智能批改"是假 AI、工作台快捷入口全部死链 |
| 🛡️ 健壮性 | 🟠 偏弱 | 假登录+默认密码硬编码、无全局错误捕获、file:// 多处静默失败、文档严重脱节 |

---

## 二、功能维度问题（按优先级）

### 🔴 P0 — 核心功能名不副实 / 入口失效
1. **作文「智能批改」是随机假 AI**
   - `js/modules/essay.js:293-304`：分数 `Math.floor(Math.random()*20+40)` 随机，亮点/评语从固定数组随机取，**无任何真实分析或接口**。
   - 风险：老师用户会以为真的做了智能评分，是信任与口碑问题。
   - 建议：要么接真实大模型评分接口（需后端），要么**改名为"自动初评/快速评分"并标注"随机示例"**，避免误导。

2. **工作台 6 个快捷入口全部失效**
   - `js/modules/dashboard.js:46-55` 调用 `window.switchModule(mod)`，但全项目**不存在 `switchModule`**（app.js 只有 `showModule` 且未挂 window）；且 `data-module` 用 `lesson-plan` 而路由键是 `lesson`。
   - 风险：用户点快捷卡片无反应，体验直接崩。
   - 建议：统一为 `window.showModule('lesson')` 等真实路由键，或暴露 `switchModule` 别名。

### 🟠 P1 — 明显功能缺陷
3. **作文「复制报告」未真正复制** — `essay.js:481` 仅 `alert('报告已生成…')`，未执行复制。建议用 `navigator.clipboard.writeText`。
4. **教材资源「新增篇目」是占位** — `resource.js:654` 弹 `alert('功能开发中，敬请期待。')`。建议要么实现，要么从 UI 隐藏入口。
5. **数据分析「班级筛选」强制跳回报表** — `data-analysis.js:549-557`：无论当前在"学情分析/趋势图"子视图，切换班级都重渲染综合报表，体验异常。建议按当前子视图刷新。

### 🟡 P2 — 体验/可维护性
6. **多个列表无分页**：resource（132 篇一次渲染）、class（学生表）、data-analysis（成绩表）均整表渲染，数据量大时卡顿。建议加分页或虚拟滚动。
7. **班级管理交互粗糙**：`class.js` 用一连串 `prompt/confirm` 添加班级、请假审批，建议改为模态表单；并补"班级编辑/删除"。
8. **试卷作者字典写死**：`exam.js:285+` 约 60 位作者信息内联在模块里，体量大、难维护，建议抽到独立数据文件。

---

## 三、内容维度问题

### ✅ 健康项（已验证）
- **教材详情覆盖率 100%**：137/137 篇 `hasDetail=1`，索引与详情分片完全对齐（orphan=0, extra=0）。
- **字段完整性 0 缺失**：文言文 8 字段 / 古诗词 5 字段 / 课文 4 字段，全量校验通过。
- **难句连续性红线通过**：137 篇 hardSentences 全部为 fullText 连续子串，0 失败。
- **试卷生成完整**：8 个生成器动态组卷，题干/选项/答案/解析齐全。

### ⚠️ 需修复
1. **文档与代码严重脱节（高优先级）**
   - `_catalog.md` 仍写"29 篇完成 / 108 篇未完成"，实际已 **137/137 全完成**，落后 108 篇。
   - 文档注明"统计基于 `data/sample-data.js`"，但该文件**已删除**（数据早拆分为 `texts-index.js`+`detail-*.js`）。
   - 建议：据 `texts-index.js` 重新生成目录，或删除旧 `_catalog.md`，避免误导验收与维护。
2. **字段 schema 偏差 38 处（低）**：37 篇古诗词含 `translation`、1 篇课文（沁园春·长沙）含 `pinyinMap`，与既定规则不符。建议统一规则（允许这些字段）或清理。

---

## 四、健壮性 / 技术维度

### 🔴 P0 — 安全隐患
1. **登录鉴权为纯前端假登录**
   - `js/modules/auth.js:10-15`：默认管理员密码 `btoa('luluxiuno1')` **硬编码在源码**，任何人可登录/提权。
   - 会话 `auth_session` 存**明文 base64 密码**（`auth.js:47-54`），无签名/过期/加密。
   - 评估：作为**单机单人本地工具**可接受，但**切勿联网多人部署**。建议：首次启动强制改密 / 移除硬编码默认密码 / 明确"本地单机"定位。

### 🟠 P1 — 体验与可维护性
2. **无全局错误捕获**：全仓库无 `window.onerror`/`unhandledrejection`。建议加全局捕获 + 错误 Toast，避免静默崩。
3. **file:// 直接打开多处静默失败**（不止 OCR）：
   - `text-loader.js:62-74` 动态注入 `detail-*.js` 在 `file://` 下被 CORS 拦截 → 详情页**静默显示"暂无数据"**（与 OCR 同源问题）。
   - 建议：在 file:// 协议下给出明确降级提示（"请通过 http://localhost:3000 访问"），而非静默无数据。

### 🟡 P2 — 代码清理
4. **notification.js:84** `return notifications;` 早于 `saveNotifications` 调用 → 保存逻辑是死代码。
5. **app.js** 重复 `case 'data-analysis'`（:732 与 :753），后者不可达；且 :788-790 引用 HTML 中不存在的 DOM（`.app-header`/`.user-info`/`pendingTaskStat`），静默 no-op。
6. **PWA 未启用**：`index.html:9,88-92` 的 manifest/SW 注册被注释，`icons/` 目录缺失 → 无法安装/离线。如需 PWA，补 icons 并仅 https 环境启用。
7. **全局 window 污染严重**：每个模块挂 12–30 个函数到 window，无命名空间，碰撞风险高。建议收敛到统一命名空间。
8. **settings.js:779-784** `clearCache` 标注"模拟"但实际空实现；`getStoragePercent` 写死 5MB（Chrome 实际 ~10MB）。

---

## 五、优化建议清单（可执行 · 按优先级）

| # | 问题 | 建议改动 | 涉及文件 | 优先级 | 工作量 |
|---|---|---|---|---|---|
| 1 | 智能批改是假 AI | 改名"自动初评"并标注示例，或接真实评分接口 | essay.js | 🔴 | 小/大 |
| 2 | 工作台快捷入口死链 | 统一 `switchModule`→`showModule('lesson')` 等真实路由键 | dashboard.js / app.js | 🔴 | 小 |
| 3 | 登录默认密码硬编码 | 移除硬编码、首次强制改密、明确本地单机定位 | auth.js | 🔴 | 中 |
| 4 | 无全局错误捕获 | 加 `window.onerror`+错误 Toast | app.js | 🟠 | 小 |
| 5 | file:// 静默失败 | text-loader 加 file:// 降级提示 | text-loader.js | 🟠 | 小 |
| 6 | 复制报告不生效 | 改用 `navigator.clipboard` | essay.js | 🟠 | 小 |
| 7 | 文档严重脱节 | 重生成/删除 `_catalog.md` | _catalog.md | 🟠 | 小 |
| 8 | 资源新增篇目占位 | 实现或隐藏入口 | resource.js | 🟠 | 小/中 |
| 9 | 班级筛选跳回报表 | 按当前子视图刷新 | data-analysis.js | 🟠 | 小 |
| 10 | 列表无分页 | 加分页/虚拟滚动 | resource/class/data-analysis | 🟡 | 中 |
| 11 | 班级 prompt 交互 | 改模态表单+编辑/删除 | class.js | 🟡 | 中 |
| 12 | 作者字典写死 | 抽独立数据文件 | exam.js | 🟡 | 小 |
| 13 | notification 死代码 | 修复 `return` 顺序 | notification.js | 🟡 | 小 |
| 14 | app.js 重复 case | 删重复 case + 清理不存在 DOM 引用 | app.js | 🟡 | 小 |
| 15 | PWA 未启用 | 补 icons + 仅 https 启用 | index.html / icons/ | 🟡 | 中 |
| 16 | 全局 window 污染 | 收敛命名空间 | 多文件 | 🟡 | 大 |
| 17 | settings 空实现 | 实现 clearCache / 修正 5MB | settings.js | 🟡 | 小 |
| 18 | schema 偏差 38 处 | 统一字段规则或清理 | detail-*.js | 🟡 | 小 |

---

## 六、建议落地顺序
- **第一轮（当天可清，约 1–2 小时）**：#2 快捷入口、#4 全局错误捕获、#5 file:// 提示、#6 复制报告、#7 文档、#10 之前先做 #8/#9、#13/#14 清理。
- **第二轮（安全与体验）**：#1 智能批改改名、#3 登录改密、#11/#12 交互与数据拆分。
- **第三轮（架构）**：#15 PWA、#16 命名空间收敛、#17 设置实现。

> 注：本报告原为体检与建议；P0 三项已按下方「七、P0 修复记录」落地。如需继续推进 P1/P2，告诉我编号即可。

## 七、P0 修复记录（2026-08-04）
| # | 问题 | 改动 | 验证 |
|---|---|---|---|
| 1 | 智能批改假 AI | `essay.js`：Tab 改名「🤖 自动初评」；报告加"非 AI 精批"醒目提示；分数由纯随机改为"字数基准+±3 扰动"并显示字数 | `node --check` 通过 |
| 2 | 工作台快捷入口死链 | `dashboard.js`：`data-module="lesson-plan"`→`lesson`；点击改调真实 `showModule(mod)` | `node --check` 通过；6 个入口路由键均命中 app.js `case` |
| 3 | 登录默认密码硬编码 | `auth.js`：移除 `btoa('luluxiuno1')`，首次初始化随机生成初始密码并提示；会话去密码化（改存 token）；登录/改密后清除初始密码提示键。`app.js`：登录页显示初始密码提示 | vm 沙箱 13 项断言全绿 |

> ⚠️ 仍仅定位为**本地单机工具**：登录为前端校验，请勿联网多人部署。P1/P2 待用户指定后继续。
