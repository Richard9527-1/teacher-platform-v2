# 试卷中心改造方案

> 评估时间：2026-08-04
> 范围：教师平台「试卷中心」模块（`js/modules/exam.js` 及相关数据）的功能与题目质量改造
> 结论先行：**试卷中心当前"题目不好 / 功能简单"的根因，不是题型模板写得差，而是生成引擎只读轻量索引、没接入已补齐的详情富数据，且存在一个会让 52 篇（38%）重点篇目直接"出不了题"的键名 bug。**

---

## 一、现状盘点

| 项 | 现状 |
| --- | --- |
| UI | 选课文（可 Ctrl 多选）+ 单选题型（5 类）+ 题量（1–15）→ 智能组卷 → 渲染 + 导出 Word/JSON |
| 题型 | 选择题、默写、翻译、赏析、主观题（一次只能生成**单一题型**） |
| 数据源 | **仅 `window.SAMPLE_TEXTS`（轻量索引）**，完全未加载 `data/texts/detail-*.js` 富数据 |
| 导出 | Word（HTML 改名 `.doc`）、JSON |

### 已验证的数据事实（来自 `data/texts-index.js` 解析）
- 总篇目 137 篇；**52 篇键名带 `*` 前缀**（鸿门宴、苏武传、永遇乐、声声慢、登高、齐桓晋文之事等全部重点精读篇）。
- 索引 `original` 字段只是**开篇节选**：长度 8–86 字，均值 ~42 字，并非全文。
- 索引中带 `translation` 字段的篇目：**0 篇**。
- `背诵要求` 分布：全文背诵 49 篇 / 无 88 篇。
- 类型分布：课文 69 / 古诗词 37 / 文言文 31。
- 详情富数据（`detail-*.js`）已有字段：`fullText`、`pinyinMap`、`notes`、`wenyan`、`hardSentences`(sentence/translation/explanation/tags)、`appreciation`、`examFocus`、`translation` —— **试卷模块一个都没用上**。

---

## 二、核心问题诊断

### A. 数据与接口硬伤（导致"出不了题 / 答案是待补充"）
1. **`*` 前缀键名 bug（严重）**：`getAllTextNames()` 显示时 `replace(/^\*\s*/, '')` 去掉了 `*`，但 `generateQuestionsFromTexts` 用去掉 `*` 的名字去 `texts[name]` 查 → 查不到 → 返回"未找到选中的课文数据"。**选中这 52 篇重点篇目时整批不出题，约 38% 篇目直接废掉。**
2. **语料是节选**：默写/翻译/赏析都以 `item.original`（开篇 8–86 字）为语料 → 默写只能填开头几句、翻译只翻首句、赏析只引第一句。
3. **翻译题答案恒为"待补充"**：生成器取 `item.translation`，但索引 0 篇有该字段（31 篇文言文的精译在 detail 里，读不到）。
4. **默写强依赖 `背诵要求`**：仅 49 篇可生成默写，其余 88 篇选了就报"无法生成默写题"。
5. **富数据闲置**：已补齐的 `examFocus / hardSentences / appreciation / notes / fullText / pinyinMap` 全部未被利用。

### B. 题目内容质量（"题目也不太好"的主因）
6. **选择题全是元知识**：只考作者 / 朝代 / 文体 / 标签 / 首句出处，没有一道真正的阅读理解题；干扰项常从全员作者里随机抽，出现"作者A/B/C/D"这种与内容无关的怪选项；"文体"选项把散文/小说与文言文/诗歌混列。
7. **赏析 / 主观题答案是"模板壳"**：主观题答案铺满未填充的 `（…）` 占位（如"作者（抒发个人感慨/批判社会现实/表达政治理想）"）；赏析题 `techniqueMap` 把某句硬套成"比作另一个意象"等空泛表述——读起来像脚手架，不像参考答案。
8. **翻译题无难度区分、无采分点**，且答案缺失。
9. **一次只能出一种题型**，无法组"选择 + 默写 + 诗歌鉴赏 + 现代文阅读"混合套卷。
10. **现代文（69 篇）最弱**：缺"词语含义 / 句子作用 / 语段赏析 / 探究"等高考常考现代文阅读题型。

### C. 功能 / 体验短板
11. 无难度分层（基础/巩固/培优）、无知识点筛选；索引里 `grade`/`unit` 没利用。
12. 无套卷结构配置（不能设"选择×10 + 默写×6 + 鉴赏×2…"）、无学校/班级抬头、无分页。
13. Word 导出答案直接跟题后，**无法做"学生卷 + 答案卷"分离**；且是 HTML 改名 `.doc`，排版易乱。
14. 试卷无保存 / 命名 / 历史，刷新即丢，不能复用或二次编辑。
15. 生成后不能删题 / 调序 / 替换 / 收藏。

---

## 三、改造目标
- **能出、出对**：所有 137 篇（含 `*` 篇）均可正常出题；翻译题有真实译文；默写基于全文。
- **题目好**：以富数据为语料与答案骨架，出真阅读理解题，答案去模板化、给踩分要点。
- **好用**：支持混合套卷、难度/知识点筛选、答案卷分离、保存复用、生成后微调。

---

## 四、分阶段改造方案

### P0 — 紧急修复（让"能出、出对"）【预估 0.5 天】
1. **修 `*` 键名匹配**
   - 在 `exam.js` 增加 `lookupText(name)`：`texts[name] || texts['*'+name] || texts['* '+name]`；`getAllTextNames()` 返回 `{label, key}`，渲染用 label、查找用 key。
   - 涉及：`js/modules/exam.js`（`getAllTextNames`、`generateQuestionsFromTexts`）。
2. **接入详情富数据**
   - 复用 `js/modules/text-loader.js` 的 `getDetail(name)`（已支持按 shard 懒加载），在生成前预取所选篇目的 detail。
   - 生成器改为优先用 `detail.fullText / detail.translation / detail.examFocus / detail.hardSentences / detail.appreciation / detail.notes`，缺失时回退索引 `original`。
   - 涉及：`js/modules/exam.js`、`js/modules/text-loader.js`、`data/texts/detail-*.js`。
3. **默写语料改全文**
   - `generateClozeQuestions` 改用 `detail.fullText`（无则 `original`）；去掉"无背诵要求即报错"，改为全篇可默；新增"理解性默写"模式（结合 `examPoints` 给情境填句）。
   - 涉及：`js/modules/exam.js`。

### P1 — 题目质量重构（让"题目好"）【预估 2–3 天】
4. **选择题真阅读化**
   - 新增题库：内容理解 / 词句含义 / 手法辨识 / 信息筛选；干扰项基于同篇或同单元易混点，而非随机作者。
   - 新增"多选题"题型（答案可为多选项）。
   - 涉及：`generateChoiceQuestions`。
5. **翻译题提质**
   - 用 `detail.translation` 按句对齐给参考答案 + 重点字词采分点（取自 `detail.wenyan`/`notes`）；按难度/长度选句，不取前 N。
   - 涉及：`generateTranslationQuestions`。
6. **赏析 / 主观题去模板化**
   - 以 `detail.appreciation` / `hardSentences[].explanation` 为答案骨架，删除所有 `（…）` 占位。
   - 主观题给"要点式踩分"参考答案（1.2.3. 结构），而非空壳。
   - 涉及：`generateAppreciationQuestions`、`generateSubjectiveQuestions`、`authorInfo` 字典扩充（覆盖更多作者）。
7. **现代文阅读题型**
   - 基于 `detail.hardSentences`（已补 6 条难句 + 解析）生成：词语含义 / 句子作用 / 语段赏析 / 探究题。
   - 涉及：新增 `generateModernReadingQuestions`。
8. **默写双模式**：上下句填空 + 理解性默写（情境填句）。

### P2 — 功能增强（让"好用"）【预估 3–5 天】
9. **套卷结构配置**：题型 × 数量 × 分值自由组合 → 生成混合套卷；加年级/单元/难度筛选（用索引 `grade`/`unit`）。
10. **答案卷分离 + 规范 Word**：学生卷（无答案）+ 答案卷（含解析/采分点）；含学校/班级/姓名栏、密封线、题号分值；导出真正的 `.docx`（或用 docx 库）。
11. **试卷保存 / 命名 / 历史**：`localStorage` 持久化；可复用、可二次编辑（增删题、调序、替换、收藏）。
12. **题目微调**：生成后可单题删除 / 替换 / 收藏；导出前可预览分页。
13. **题型扩展**：文言"词语解释 / 断句"、诗歌"意象 / 语言风格"、开放探究题、微写作 / 作文（给材料 + 评分标准）。

---

## 五、验收标准
- [x] 选中任意 `*` 前缀重点篇目可正常出题，无"未找到选中的课文数据"。
- [x] 翻译题对 31 篇文言文输出真实译文 + 采分点，不再"待补充"。
- [x] 默写基于全文，且 137 篇均可生成（或明确标注不可背篇目但不报错中断）。
- [x] 选择题出现内容理解类真阅读题（主旨/名句/实词/手法/意象），干扰项取自同单元/同年级易混点（非随机作者）。
- [x] 赏析 / 主观题答案无 `（…）` 占位，以 `appreciation` / `hardSentences[].explanation` 为骨架，要点式可踩点给分。
- [x] 新增"多选题"题型（答案为多选项，渲染/导出已支持）。
- [x] 新增"现代文阅读"题型（词语含义/句子作用/语段赏析/探究，仅课文适用）。
- [x] 默写新增"理解性默写（情境填句）"双模式。
- [x] 可一次性生成混合套卷（多题型：题型×数量×分值自由组合），并支持年级/单元/难度筛选。
- [x] Word 导出学生卷与答案卷分离（学生卷含学校/班级/姓名/学号栏+密封线、无答案；答案卷独立），排版规范化（卷头/大题标题/分页结构）。

---

## 八、P0 实施记录（2026-08-04 已完成）

**改动文件**：`js/modules/exam.js`（新增 `lookupText`、`corpusOf` 辅助；改写 `generateQuestionsFromTexts`、`initExam`；调整 `generateClozeQuestions`、`generateTranslationQuestions`、`generateAppreciationQuestions`、`generateSubjectiveQuestions`、`generateChoiceQuestions` 模板5 语料）。

### 1. 修复 `*` 前缀键名 bug
- 新增 `lookupText(name)`：依次回退 `texts[name]` → `texts[clean]` → `texts['*'+clean]`，返回 `{ key, display, item }`。`generateQuestionsFromTexts` 改用它解析篇目，向各生成器传入**清洗后的显示名**（`《鸿门宴》` 而非 `《*鸿门宴》`）与已合并富数据的 item。
- 经 node 模拟校验：137 篇中 52 篇带 `*` 的篇目，lookup/组卷失败 **0**，缺 fullText **0**。

### 2. 接入富数据（异步按需加载）
- `initExam` 点击「智能组卷」后，先 `Promise.all(selectedTexts.map(window.loadTextDetail))` 加载所选篇目详情分片（text-loader 已把 fullText/translation/hardSentences 等合并进 `SAMPLE_TEXTS`），加载完成后再组卷；加载中显示"正在加载…"提示，失败有兜底报错。
- 仅加载所选篇目所属分片，避免一次性拉全库，符合方案"按需懒加载"要求。

### 3. 默写改用 fullText 全文语料
- `generateClozeQuestions` 去掉"无背诵要求即报错"的限制，语料由 `item.original`（节选）改为 `corpusOf(item)`（`item.fullText || item.original`）。**137 篇均可基于全文出默写/挖空题**，诗词默写为上下句、散文/文言为句中挖空的逻辑保持不变。

### 4. 翻译题用 hardSentences 精准对齐
- `generateTranslationQuestions` 改为从 `item.hardSentences` 取（`sentence` 为 fullText 连续子串、`translation` 为对应译文），答案给该句译文 + 采分点（tags）；无 hardSentences 译文时回退 `detail.translation` 整段按句切分。
- 现代文（type==='课文'）明确提示"无需翻译题"；其余类型不再"待补充"。
- 抽样（劝学/鸿门宴/苏武传/登高/永遇乐）异常 **0**。

### 5. 赏析/主观/选择题语料切到 fullText
- `generateAppreciationQuestions`、`generateSubjectiveQuestions` 的语料由 `item.original` 改为 `corpusOf(item)`；选择题"名句出处"模板同步，引用的句子来自全文而非开篇节选。

### 校验方式
- `node --check` 语法通过。
- 用 node 模拟 text-loader 的"索引+6 分片合并"逻辑，再将 `exam.js` 载入 VM 实际调用 `generateQuestionsFromTexts`：覆盖 `*` 解析、翻译真译文、现代文/诗词默写、全题型 `*篇目` 零崩溃，全部通过。
- 临时校验脚本已清理。

### 未覆盖（留待 P1）
- 选择题仍为元知识类（作者/朝代/文体/主题/名句出处），内容理解类真阅读题、合理干扰项、赏析/主观题去占位重构、现代文阅读题型、混合套卷配置、答案卷分离等均属 P1/P2。P0 仅解决"能出、出对"。

---

## 六、风险与注意
- **首屏性能**：P0 接入 detail 后，组卷前需加载所选篇目富数据。detail 文件较大（137 篇分片），建议按所选篇目**按需懒加载**（text-loader 已支持），避免一次性加载全库。
- **答案准确性**：基于富数据生成的参考答案应标注"AI/系统生成，请教师核对"，尤其赏析/主观题要点式答案需教师确认。
- **不破坏现有索引契约**：`texts-index.js` 由 `_split.js` 生成，改造试卷模块不要反向改索引结构；`translation` 等富字段应在 detail 侧补全而非塞回索引。
- **向后兼容**：导出 Word/JSON 既有调用方尽量保留，新增"答案卷"为可选模式。

---

## 七、建议落地顺序
P0（修 bug + 接富数据 + 默写改全文）→ P1（选择题/翻译/赏析/主观/现代文 重构质量）→ P2（套卷配置/答案卷分离/保存复用）。**P0 落地后即可从根本上解决"出不了题/翻译待补充"，P1 解决"题目不太好"。**

---

## 九、P1 实施记录（2026-08-04 已完成）

**改动文件**：`js/modules/exam.js`（全量重写生成器；新增 `asArr` 归一化、多选题、现代文阅读、双模式默写、去模板化赏析/主观；扩充 `buildAuthorInfo` 作者字典至约 60 位；UI 增加"多选题/现代文阅读"题型与"默写模式"切换）。

### 1. 选择题真阅读化（原 4 号项）
- 新增内容理解题库，均以详情富数据为语料/答案骨架：
  - **主旨理解**：正确项 = `《篇》` + `appreciation.theme`，干扰项取自**同单元/同年级**他作 `theme`（易混点，非随机作者）。
  - **名句理解**：正确项 = `hardSentences[].explanation`，干扰项取自本篇他句/他作难句解析。
  - **文言实词**：正确项 = `notes[词]`，干扰项取他词释义（并附原句语境 `sentenceContaining`）。
  - **手法/艺术特色**：正确项 = `《篇》` + `appreciation.features`，干扰项取他作特色。
  - **意象/意境**（诗词）：以难句 `explanation` 为正确项。
  - 保留作者/朝代/文体元知识题，但干扰项已改为同侪干扰项。

### 2. 多选题（原 4 号项新增题型）
- 新增 `generateMultipleChoiceQuestions` 与 UI 选项"选择题（多选）"；答案为数组（如 `A、C`），渲染/导出均支持。
- 三类变体：艺术特色多选、内容多选（主旨+思路）、名句理解多选（难句解析），每篇最多可出 3 道。

### 3. 翻译题提质（原 5 号项）
- 保留 hardSentences 对齐译文；新增**重点字词采分点**：句中命中 `notes` 的实词释义 + `wenyan.keyWords` 虚词 + `tags` 考点。
- 选句改为按句长排序取中段（更有翻译/采分价值），不再只取前 N。

### 4. 赏析 / 主观题去模板化（原 6 号项）
- `generateAppreciationQuestions` / `generateSubjectiveQuestions` 均以 `appreciation` / `hardSentences[].explanation` / `examFocus` 为骨架，**全文删除 `（…）` 占位**。
- 主观题改为**要点式踩分**（1. 2. 3. 结构）：知人论世（作者字典扩充）、文本细读、手法综合、情感脉络、现实意义。
- `buildAuthorInfo` 字典由 14 位扩充至约 60 位（含古典/现代/外国作家），其余走无占位兜底。

### 5. 现代文阅读题型（原 7 号项）
- 新增 `generateModernReadingQuestions` 与 UI 选项"现代文阅读"（仅 `课文` 适用，否则友好报错）。
- 基于 `hardSentences` + `appreciation` 生成：词语含义（语境）、句子作用、语段赏析、开放探究。

### 6. 默写双模式（原 8 号项）
- `generateClozeQuestions` 增加 `mode` 参数：默认"直接默写"（上下句/句中挖空，沿用 P0 全文语料）；新增"理解性默写（情境填句）"——以 `examFocus.dictation` 指定句为答案，情境取自对应难句 `explanation` 或 `appreciation.theme`。
- UI 增加"默写模式"单选（仅题型=默写时显示）。

### 校验方式
- `node --check` 语法通过。
- VM 模拟"索引 + 6 分片加载"后实际调用各生成器，覆盖**各文体代表篇目 + 6 个 `*` 篇目 + 全 7 种题型 + 双默写模式 + 类型不匹配边界**：
  - 生成 150 题，**抛异常 0**、**`（…）` 占位残留 0**、**翻译题无采分点 0**、**多选题答案非数组 0**。
- 修复的关键隐患：详情数据中 `appreciation.features` / `famousLines`（48 篇）与 `examFocus.dictation`（12 篇）部分为**整段字符串**而非数组；新增 `asArr` 归一化（描述型字段整段作单项，dictation 按换行/顿号拆句），避免 `.forEach`/`.join` 崩溃。
- 回归脚本保留：`_p1_test.js`。

### 未覆盖（留待 P2）
- 混合套卷结构配置（题型×数量×分值）、难度/年级筛选、答案卷分离 + 规范 `.docx`、试卷保存/命名/历史、生成后单题微调（删/调序/替换/收藏）。P1 仅解决"题目好"，套卷工程化属 P2。

---

## 十、P2 实施记录（2026-08-04 已完成 · 套卷结构）

**改动文件**：`js/modules/exam.js`（新增 `getTextMeta`/`difficultyOf`/`sortGrades`/`typeLabelOf`/`cnNum`/`sectionRowHtml` 辅助；抽出 `generateSectionQuestions`；新增 `generateMixedPaper`；重写 `renderExam`/`renderQuestions`/`exportToWord`/`exportToJson`/`initExam`）。

### 1. 混合套卷结构配置（题型 × 数量 × 分值）
- 新增 `generateMixedPaper(textNames, sections, clozeMode)`：`sections` 为 `[{type, count, score}]` 数组，遍历各题型调用 `generateSectionQuestions`（由旧 `generateQuestionsFromTexts` 抽出的单题型生成器），按大题聚合、累加 `score × generated` 得到总分与满分，返回 `{ meta, sections }`。
- `generateQuestionsFromTexts` 保留为兼容入口（调用 `generateSectionQuestions` 返回扁平题数组），旧调用方与 `_p1_test.js` 不受影响。
- UI 由"单题型下拉 + 题量"改为**试卷结构构建器**：默认 3 大题（选择题×10×3 / 默写×6×5 / 赏析×2×10），支持「＋ 添加题型」增行、每行「✕ 删除」、题型/题量/每题分值可改；实时显示全卷合计题数/分值。默写模式行按是否含默写大题联动显隐。

### 2. 年级 / 单元 / 难度筛选
- `getTextMeta()` 从索引 `grade`/`unit` 动态提取年级、单元；`difficultyOf(grade)` 由年级派生难度（高一=基础、高二=提高、高三=培优）。
- `renderExam` 新增三个筛选下拉（年级 / 单元 / 难度（按年级）），`initExam` 中 `applyTextFilter()` 在筛选变化时收窄 `#examText` 课文列表（保留仍匹配项的已选状态）。

### 3. 渲染与导出支持套卷分节
- `renderQuestions` 兼容 `paper` 对象：卷头显示"全卷共 N 题（应 M 题），满分 X 分"；各题型以"第X大题　题型（每小题Y分，共Z分）"为标题；**跨大题连续编号**；某大题 0 产出时给出黄色提示而非崩溃；并保留旧扁平数组兼容分支。
- `exportToWord`/`exportToJson` 同样兼容 `paper`：Word 按大题标题分页、含总分信息；JSON 输出 `meta + sections[]`（每节含 type/score/requested/generated/title/questions）。
- 全失败（generatedCount=0）时 `generateMixedPaper` 返回 `{error}`，`renderQuestions` 直接展示"⚠️ 组卷失败：…"，避免丢失错误原因。

### 校验方式
- `node --check` 语法通过。
- 新增 `_p2_test.js`（VM 加载 索引+loader+6分片+exam.js）：覆盖 `getTextMeta` 元数据、难度筛选与年级一致性、7 大题混合套卷（总分/分值一致、大题标题格式）、翻译（非课文产出）/现代文阅读（课文产出）类型匹配、理解性默写 mode、类型不匹配边界（古诗词+现代文阅读大题优雅降级）、空配置防御——**全部通过**。
- 重跑 `_p1_test.js`：149 题、**0 异常 / 0 占位 / 0 翻译无采分点 / 0 多选非数组**，确认无回归。

### 4. 答案卷分离 + 规范 Word 导出（2026-08-04 补充）
- 重构 `exportToWord`：抽出纯函数 `buildWordHtml(paper, opts)`，按 `mode` 输出三种文档：
  - `student`（学生卷）：卷头含**学校/班级/姓名/学号栏 + 密封线**，题目不含任何答案与提示；
  - `answer`（答案卷）：仅输出「题号 + 答案」（选择题附选项标注），供教师阅卷；
  - `full`（含答案试卷）：保留题目 + 参考答案/正确选项，维持原行为。
- UI 导出按钮由「导出Word / 导出JSON」改为「导出学生卷 / 导出答案卷 / 导出JSON（含答案）」，`initExam` 分别接线对应 mode；文件名带「学生卷/答案卷/试卷」后缀。
- 排版规范化：卷头总分信息、大题标题（第X大题…）、题号分值、密封线样式；导出格式仍为 Word 兼容的 `.doc`（HTML 命名），非严格 OOXML `.docx`（如需真 `.docx` 可后续引入 docx 库，属可选增强）。

### 未覆盖（留待 P2 其余项 / 后续）
- 试卷保存/命名/历史（localStorage 持久化、可复用二次编辑）、生成后单题微调（删/调序/替换/收藏）、更多题型扩展（文言词语解释/断句、开放探究、微写作）仍属方案 P2 其余条目，本次未做（答案卷分离已完成）。
- 题量在所选篇目数据不足时为"尽力产出"（如现代文阅读 1/3、翻译 2/3 属正常上限），UI 与导出均标注"应 M 题 / 实 N 题"。

